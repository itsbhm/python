# Tuples

# Note: We can not Modify(Edit, Delete) Tuples
tup = ('A', 'B', 'C')

# tup[0] = 'X' (Can't Modify)

print(tup[0])

print(tup[0:2])

newTup = ('X', 'Y', 'Z')

finalTup = tup + newTup

print(finalTup)

print(tup * 2)
